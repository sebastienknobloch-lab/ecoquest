# Audit EcoQuest — 2026-09-13

Périmètre : commits du 11 au 13 septembre 2026 sur `main`. 22 commits, 10 items cochés (tous dans `ROADMAP-2.md`, aucun dans `ROADMAP.md`). Tests : 6/6 au vert.

## Critique

Aucun. Pas de secret dans le repo, pas de fuite possible entre foyers (aucun backend à ce stade), aucune migration destructrice, aucune CI à casser.

## À corriger

1. **[Axe 3 — dérive]** Deux CLAUDE.md et trois ROADMAP.md cohabitent : les versions à jour du 13/09 sont `CLAUDE-1.md` et `ROADMAP-2.md`, mais les fichiers lus à chaque session sont les versions périmées `CLAUDE.md` et `ROADMAP.md` (0 case cochée sur 31). L'architecture cible n'en connaît qu'un de chaque. Fichiers : CLAUDE.md, ROADMAP.md, CLAUDE-1.md:22, ROADMAP-2.md:68-77, architecture CLAUDE-1.md:43-67.
2. **[Axe 3 — dérive]** 25 des 30 gestes portent `a_verifier: true`, mais ce champ n'est lu nulle part dans le code (0 occurrence) : leur chiffre CO2 s'affiche comme celui des gestes sourcés et alimente l'impact cumulé. Fichiers : js/views/aujourdhui.js:155, js/gamification.js:358, js/views/profil.js:43.
3. **[Axe 4 — tests]** `js/state.js` est le seul module sans test, alors que `loadState()` porte les trois migrations qui protègent les données. Fichiers : js/state.js:24-66, tests/ (aucun state.test.js).
4. **[Axe 4 — CI]** Aucun `.github/workflows/` : rien n'exécute les tests. Le prompt de S11 prescrit `node --test tests/`, commande qui échoue sur Node 22 (« Cannot find module .../tests ») ; la bonne forme est `node --test` depuis la racine. Fichier : ROADMAP-2.md:83.
5. **[Axe 6 — accessibilité]** Badges verrouillés : texte #8a8a8a sur fond #eceae4 à 2,87:1, et `opacity: 0.75` fait tomber le rendu réel à 2,10:1 pour un AA à 4,5:1. Fichier : css/app.css:382-391.
6. **[Axe 6 — qualité]** Cocher un geste depuis le catalogue referme toutes les catégories dépliées : `basculerGeste()` appelle `afficherTout()`, qui recrée les `
Détails
` et perd leur attribut `open`. Fichiers : js/views/aujourdhui.js:127, :192-200.
7. **[Axe 6 — duplication]** La liste des 5 catégories existe à trois endroits et l'affichage du catalogue suit la constante de la vue, pas les données : une catégorie ajoutée au JSON disparaîtrait silencieusement. Fichiers : js/views/aujourdhui.js:4-10, js/gamification.js:228, tests/points.test.js:11.

## À surveiller

8. **[Axe 1]** Le service worker met en cache toute réponse GET sans liste blanche. Inoffensif aujourd'hui (aucune donnée utilisateur sur le réseau), à cadrer avant l'arrivée de Supabase en S41. Fichier : sw.js:37-52.
9. **[Axe 2]** `DEFAULT_STATE.streak` et `.joker` pointent sur les constantes de module partagées ; les trois chemins de repli de `loadState()` renvoient un état qui les partage. Sans effet tant que le code reste immuable. Fichier : js/state.js:11-12, :29, :32, :64.

## Écarts de suivi

- Aucun item coché sans code : les dix cases de `ROADMAP-2.md` correspondent au code présent.
- Dix items livrés sans case cochée dans `ROADMAP.md`, le fichier que désigne l'architecture cible (constat 1).
- `changelog.md` est à jour côté code jusqu'à la session 10. En revanche, le changement de cap des 12-13/09 (rythme 1 h/jour, stack Capacitor + Supabase, notification au cœur du produit, roadmap à 69 items) n'y figure pas.
- Deux affirmations du changelog ne correspondent plus au code : « contraste AA sur les badges verrouillés » (session 9) et « seule la première (Énergie) ouverte par défaut » (session 5).

## Rien à signaler sur

- **Axe 1** — aucun secret, clé, mot de passe ou keystore dans le repo ; aucun workflow, donc aucun log de CI. Cloisonnement des foyers sans objet : pas encore de backend.
- **Axe 2** — les six propriétés d'état ajoutées cette semaine ont toutes leur valeur par défaut dans `loadState()` ; la migration `completedToday` ne peut pas écraser un historique existant. Export/import sans objet (S14 non fait).
- **Axe 3** — zéro build tenu (aucun import externe, aucune dépendance npm). Dérivé jamais stocké : niveau, badges, impact et équivalences sont tous recalculés à la volée. Les 30 gestes et les 3 équivalences ont un champ `source` non vide.
- **Axe 4** — 6/6 tests au vert, aucun test ignoré, vidé ou marqué `skip`/`todo`/`only`.
- **Axe 6** — cibles tactiles ≥ 44 px partout ; contrastes conformes hors badges verrouillés. Pas de code mort significatif.
- **Axe 7** — sans objet : phase 3 non commencée, aucun appel à l'API Notification dans le code.
