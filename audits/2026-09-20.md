# Audit EcoQuest — 2026-09-20

Périmètre : commits du 13 au 19 septembre 2026 sur `main`. 32 commits hors merges, 28 cases cochées dans `ROADMAP.md` dont 18 cette semaine (S11 → S28). Tests : 13/13 au vert sur Node 22.

## Critique

Aucun. Pas de secret dans le repo ni dans l'historique git, pas de fuite possible entre foyers (aucun backend à ce stade), aucune migration destructrice, CI des tests au vert.

## À corriger

1. **[Axe 2 — perte de données]** `importerEtatJSON()` n'applique pas la migration douce que `loadState()` applique ligne 78 : une sauvegarde sans champ `onboarding` (tout export réalisé entre S14 et S18) est réimportée avec `onboarding.termine: false`. Points et historique sont conservés, mais `js/app.js:66` renvoie l'utilisateur dans l'onboarding au prochain lancement, prénom et catégories prioritaires remis à zéro. Vérifié par simulation. Fichiers : js/state.js:202-210, js/state.js:78-80, js/app.js:66.

2. **[Axe 3 — dérive]** `renderOnboarding` construit la liste des catégories à choisir depuis la constante `LIBELLES_CATEGORIES` de la vue, pas depuis `data/gestes.json` — exactement la régression corrigée le 13/09 pour le catalogue (commit 4ecd458), réintroduite trois jours plus tard en S18. Latent aujourd'hui (les 5 clés correspondent aux 5 catégories du catalogue), actif dès qu'une catégorie bouge : un choix devenu invalide fait silencieusement retomber `selectionDuJour()` sur le tirage aléatoire. Fichiers : js/views/onboarding.js:96, js/views/aujourdhui.js:8-14, js/gamification.js:236.

3. **[Axe 2 — perte de données]** Les vues écrivent un instantané de l'état. `js/views/aujourdhui.js:21` capte l'état au montage et `:129-130` le réécrit tel quel, pendant que le gestionnaire global d'erreurs alimente l'état détenu par `app.js`. Toute erreur JS capturée pendant que l'écran Aujourd'hui est affiché disparaît dès que l'utilisateur coche un geste — c'est-à-dire au moment précis où elle a le plus de chances de survenir. Même schéma dans l'onboarding. Fichiers : js/views/aujourdhui.js:21, :129-130, js/erreurs.js:51, js/app.js:137, js/views/onboarding.js:18-26.

4. **[Axe 6 — qualité]** `APP_VERSION` est figé à `"1.0.0"` alors que le `versionName` du build vient du tag Git (dernier tag : `v0.1.6`). L'écran Profil et le déclencheur « 5 taps » affichent donc une version qui ne correspond à aucun build publié. Avec 12 testeurs qui vont remonter des bugs, rien ne permet de savoir quel build tourne sur un téléphone donné. Fichiers : js/version.js:3, js/views/profil.js:228, .github/workflows/android.yml:75.

5. **[Axe 1 — service worker]** Stratégie cache-first appliquée à toutes les requêtes GET, avec mise en cache de la réponse quelle qu'elle soit : un 404 ou un 500 transitoire entre dans le cache de l'app shell et y est resservi jusqu'au prochain renommage de `CACHE_NAME`. Les réponses cross-origin y entrent aussi (Eruda depuis esm.sh / jsDelivr, ajouté en S12). Aucune donnée utilisateur ne transite aujourd'hui, mais le comportement par défaut doit être cadré avant Supabase (S41). Fichiers : sw.js:46, sw.js:49-53, js/debug.js:8-11.

6. **[Axe 6 — accessibilité]** La chaîne `source` complète est affichée sous chaque geste, dans les 3 gestes du jour comme dans le catalogue. Depuis S15/S16 ces sources font 126 caractères en moyenne et jusqu'à 206 (« réduction d'environ 7 % de la consommation par degré baissé, pour un logement chauffé au gaz (estimation ; effet environ 3 fois plus faible pour un chauffage électrique) »). CLAUDE.md:23 exige des textes compréhensibles par un enfant de 10 ans, sur un écran de 360 px. Fichiers : js/views/aujourdhui.js:163-165, data/gestes.json.

7. **[Axe 3 — dérive]** Deux instructions des fichiers de pilotage ne correspondent plus au code, et ces fichiers sont relus à chaque session : `CLAUDE.md:32` annonce `webDir` = racine du repo alors que `capacitor.config.json:4` vaut `"www"` depuis le correctif du 17/09, et `ROADMAP.md:98` (S21) prescrit l'appId `app.ecoquest` alors que le code porte `com.version012.ecoquest` depuis le 19/09. Risque concret : une prochaine session « corrige » `capacitor.config.json` vers `"."` et recasse le build. L'architecture de CLAUDE.md:45-67 ignore par ailleurs js/debug.js, js/erreurs.js, js/version.js, js/views/debug.js, .github/gradle/, package.json, docs/ et audits/. Fichiers : CLAUDE.md:32, CLAUDE.md:45-67, ROADMAP.md:98, capacitor.config.json:2-4.

## À surveiller

8. **[Axe 4 — CI]** `android.yml` ne se déclenche que sur tag, et `package.json` demande `^8.0.0` sans lockfile : toute régression du workflow ou de la résolution Capacitor n'est découverte qu'au moment de la release. C'est exactement ce qui a produit les quatre correctifs successifs de la semaine (webDir, appId, Capacitor 7→8, versionCode). Pas d'action immédiate — mais si un cinquième correctif du même type arrive, un job de validation sur PR devient moins coûteux que le cycle tag/échec/retag. Fichiers : .github/workflows/android.yml:11-14, package.json:6-12.

9. **[Axe 2 — état partagé]** `DEFAULT_STATE.streak`, `.joker`, `.onboarding` et `.erreurs` pointent sur les constantes de module ; les trois chemins de repli de `loadState()` renvoient un état qui les partage. Sans effet tant qu'aucun code ne mute l'état en place. Constat déjà signalé le 13/09, toujours ouvert. Fichiers : js/state.js:16-24, :40, :43, :84.

*2 constats mineurs écartés : le garde `a_verifier` devenu inatteignable depuis S15 (volontaire, à conserver tant que le catalogue peut grandir) et l'horodatage UTC dans le nom du fichier d'export (js/views/profil.js:16).*

## Écarts de suivi

- **Aucune case cochée sans code.** S11 à S22 et S26 correspondent toutes à du code présent et vérifié ; S23, S24, S25, S27 et S28 sont des items hors session ou des jalons, sans code attendu.
- **H2 n'est pas cochée alors que S25 l'est** — les deux désignent le même keystore, et `android.yml` signe déjà depuis `ANDROID_KEYSTORE_BASE64`. Même incohérence pour H1 et H3, non cochées alors que S27 (fiche Play Store) et S28 (12 testeurs inscrits) le sont.
- **Le jalon S28 est coché depuis le 17/09** alors que le tableau des jalons (ROADMAP.md:46) place « Test fermé ouvert, 12 testeurs recrutés » au 24 septembre. C'est le compteur des 14 jours qui dépend de cette date : soit le jalon est en avance d'une semaine, soit la case a été cochée par anticipation.
- **Le backlog d'audit est resté vide.** ROADMAP.md:178-182 le dit « (vide au 13/09/2026) », alors que l'audit du 13/09 a produit 7 constats à corriger. Six ont bien été traités dans la semaine, mais aucun n'est devenu un item `A1`, `A2`… comme le prescrit la section elle-même : le suivi passe par le changelog, pas par la roadmap.
- **La règle d'archivage des audits se contredit.** La même section annonce un rapport « en artifact (pas de fichier dans le repo) », mais `audits/2026-09-13.md` a été committé le 16/09. Une des deux règles doit sauter.
- **Deux chiffres périmés dans les fichiers de pilotage** : CLAUDE.md:85 annonce « catalogue de 30 gestes » et ROADMAP.md:72 (S5) « catalogue étendu à 30 gestes » — il y en a 20 depuis S15. Et CLAUDE.md:87-93, « Dette connue, à traiter en priorité », liste 5 points désormais tous traités (S11, S14, S15, S16, S17).
- **`changelog.md` est à jour.** Les 32 commits hors merges de la semaine sont couverts, jusqu'au correctif `versionCode` du 19/09 inclus. Aucun retard.

## Rien à signaler sur

- **Axe 1 — secrets.** Aucun keystore, mot de passe, clé ou `.env` dans les fichiers suivis ni dans l'historique git. `android.yml` ne passe les secrets que par variables d'environnement, avec `set +x` sur les deux étapes sensibles, et supprime le keystore déchiffré en `if: always()` (android.yml:82-84). `signing-init.gradle` lit tout via `System.getenv` sans jamais écrire ni afficher les valeurs. Cloisonnement entre foyers sans objet : aucun backend (phase 4 non commencée).
- **Axe 2 — valeurs par défaut.** Les deux propriétés d'état ajoutées cette semaine, `erreurs` (S13) et `onboarding` (S18), ont bien leur valeur par défaut dans `DEFAULT_STATE`. Les trois migrations douces de `loadState()` ne peuvent écraser aucune donnée existante, et un import invalide n'est jamais appliqué (l'état existant reste intact).
- **Axe 3 — zéro build et dérivé.** Sur un clone neuf, `node --test tests/*.test.js` passe sans aucun `npm install` ; `package.json` ne sert qu'à la CI. Niveau, badges, impact, équivalences et calendrier sont tous recalculés à la volée, rien de dérivé n'est stocké. Les 20 gestes et les 2 équivalences ont un champ `source` non vide, et plus aucun `a_verifier: true` ne subsiste dans le catalogue.
- **Axe 4 — tests.** 13 fichiers, 13/13 au vert, 0 skipped, 0 todo, aucun `.only`, aucun test vidé de son contenu. `tests.yml` se déclenche sur push `main` et sur chaque PR, et échoue réellement en cas d'assertion fausse. La logique ajoutée cette semaine est couverte : migrations, export/import, erreurs, debug, calendrier, catégories prioritaires.
- **Axe 7 — notifications.** Sans objet : phase 3 non commencée (S29 à S38 non cochés), pas de `js/notifications.js`, aucun appel à l'API Notification ni à `@capacitor/local-notifications` dans le code. Aucun chemin ne peut donc envoyer deux notifications le même jour, ni demander l'autorisation avant la première validation de geste.
